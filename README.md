# In order to run program:
- use the build.sh script to build program first.
- use the frontend.sh script (in the server folder) in order to run the frontend.
- use the replica.sh script (in the server folder) in order to create a replica.
- use the client.sh script (in the client folder) in order to run the client.